<section>
<article>
  <span class="date"> 
      <%= @post.created_at.strftime("%Y-%m-%d ") %>
  </span>
  <h2>
  <%= @post.title %>
  </h2>
  <p>
      <%= @post.content %>
  </p>
  <div class="btn-border-bottom">
    <%=link_to("削除","/posts/#{@post.id}/destroy",{method:"post"})%> 
  </div>
 </article>
</section>