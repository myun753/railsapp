<section>
<% @posts.each do |post| %>
<article>
  <span class="date"> 
      <%= post.created_at.strftime("%Y-%m-%d ") %>
  </span>
  <h2>
  <%= post.title %>
  </h2>
  <p>
      <%= post.content %>
  </p>
  <a href="#" class="btn-border-bottom">BUTTON</a>
 </article>
<% end %>
</section>